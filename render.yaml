services:
  - type: web
    name: neon-photo-booth
    env: python
    region: oregon  # or "frankfurt" for EU
    buildCommand: "pip install -r requirements.txt"
    startCommand: "gunicorn app:app"
    plan: free
    autoDeploy: true
